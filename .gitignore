# ---------------- 依赖 ----------------
node_modules/
.venv/

# ---------------- 密钥与本地运行数据（严禁提交）----------------
# 只排除根目录的 data/ —— 里面有 Ed25519 私钥（platform-key.json）和 secret.json
# 注意：deploy-online/data/ 与 deploy-static/data/ 是接口快照，需要保留，故用前导斜杠限定
/data/

# ---------------- 中间产物 / 临时文件 ----------------
preview/
tools/_*
tools/video/frames/
tools/video/audio/
tools/video/segs/
tools/video/_*

# ---------------- 打包产物（源文件已在仓库内，避免重复）----------------
提交/
# GitHub Pages 专用的静态包：和 deploy-static/ 只差路径前缀，且已发布在 gh-pages 分支上，
# 留在 main 里纯属重复。要用时一行命令重建（见 README「线上演示」一节）。
deploy-pages/

# ---------------- 杂项 ----------------
*.log
.DS_Store
Thumbs.db
